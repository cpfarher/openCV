------------------------------------------------------------------------
Execute project (need openCv Installed in the system).
1- Go to project
2- ./{Project}/Debug/main where {Project} is the project folder name
	e.g. ./pointsTracking/Debug/main
	e.g. ./surf/Debug/main param1 param2

------------------------------------------------------------------------
Projects in openCV git repository:

* pointsTracking	=> Lucas-Kanade algorithm
* surf			=> surf algorithm (static images)
------------------------------------------------------------------------

For compile examples of OpenCv installed on ubuntu with aptitude command:
/usr/share/doc/opencv-doc/examples/c/c$ ./build_all.sh 

